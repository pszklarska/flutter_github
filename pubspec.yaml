name: app
description: A sample Flutter project using GitHub API

dependencies:
  flutter:
    sdk: flutter
  fluro: "^1.2.1"
  url_launcher: "2.0.2"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
